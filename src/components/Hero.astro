---
---

<section id="hero" class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-14 pb-20 flex flex-col items-center text-center">
  <!-- Status Pill Badge -->
  <div class="inline-flex items-center gap-2 px-3.5 py-1.5 rounded-full bg-emerald-950/80 border border-emerald-500/40 text-emerald-400 text-xs font-mono mb-8 backdrop-blur-md shadow-lg shadow-emerald-500/10">
    <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
    <span data-i18n="badge_status">AI Graduate @ MMU | CGPA 3.74</span>
  </div>

  <!-- Sleek Dark Glassmorphism Hero Container Card -->
  <div id="vp-container" class="relative max-w-4xl mx-auto backdrop-blur-xl bg-black/75 border border-emerald-500/30 rounded-3xl p-8 sm:p-12 shadow-[0_0_50px_rgba(16,185,129,0.15)] transition duration-300 hover:border-emerald-500/50">
    
    <!-- React Bits VariableProximity Interactive Headline -->
    <h1 class="text-3xl sm:text-5xl lg:text-6xl font-bold tracking-tight mb-6 leading-tight select-none cursor-default font-['Roboto_Flex']">
      <span class="vp-word text-white" data-word="Full-Stack">Full-Stack</span>{" "}
      <span class="vp-word text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 via-teal-300 to-emerald-500 font-extrabold" data-word="Developer">Developer</span>{" "}
      <br class="hidden sm:inline" />
      <span class="vp-word text-white" data-word="with">with</span>{" "}
      <span class="vp-word text-white" data-word="a">a</span>{" "}
      <span class="vp-word text-white" data-word="Deep">Deep</span>{" "}
      <span class="vp-word text-white" data-word="Passion">Passion</span>{" "}
      <span class="vp-word text-white" data-word="for">for</span>{" "}
      <span class="vp-word text-amber-400 font-extrabold" data-word="AI">AI</span>
    </h1>

    <p class="text-base sm:text-lg text-emerald-200/90 max-w-2xl mx-auto mb-8 font-sans leading-relaxed" data-i18n="hero_desc">
      I'm <strong class="text-emerald-400 font-mono">Lim Wei Jian</strong>, an AI Graduate from MMU (CGPA 3.74) with a deep passion for full-stack engineering. Whether it's C++, Python, or TypeScript, I just enjoy turning ideas into smooth, useful software. Take a look around!
    </p>

    <div class="flex flex-wrap items-center justify-center gap-4 font-mono text-sm">
      <a href="#skill-studio" class="px-6 py-3 rounded-xl bg-emerald-500 text-black font-bold hover:bg-emerald-400 transition shadow-lg shadow-emerald-500/25 flex items-center gap-2" data-i18n="btn_skills">
        Explore Skill Studio 🚀
      </a>
      <a href="#n8n-studio" class="px-6 py-3 rounded-xl bg-emerald-950/80 border border-emerald-500/40 text-emerald-300 font-bold hover:bg-emerald-900/60 transition flex items-center gap-2" data-i18n="btn_workflow">
        Try AI Workflow 🤖
      </a>
    </div>
  </div>
</section>

<script>
  // VariableProximity Algorithm for Roboto Flex Variable Font
  const container = document.getElementById('vp-container');
  const words = document.querySelectorAll('.vp-word');
  const radius = 120; // proximity radius in px

  // Wrap letters in spans if not already wrapped
  words.forEach(word => {
    const text = word.textContent || '';
    word.innerHTML = '';
    text.split('').forEach(char => {
      const span = document.createElement('span');
      span.className = 'vp-letter inline-block transition-transform duration-75';
      span.style.fontVariationSettings = "'wght' 400, 'opsz' 9";
      span.textContent = char;
      word.appendChild(span);
    });
  });

  const letters = document.querySelectorAll('.vp-letter');
  let mouseX = -999;
  let mouseY = -999;

  window.addEventListener('mousemove', (e) => {
    if (!container) return;
    const rect = container.getBoundingClientRect();
    mouseX = e.clientX - rect.left;
    mouseY = e.clientY - rect.top;
  });

  function animateProximity() {
    if (container) {
      const containerRect = container.getBoundingClientRect();

      letters.forEach(letter => {
        const el = letter as HTMLElement;
        const rect = el.getBoundingClientRect();
        const letterX = rect.left + rect.width / 2 - containerRect.left;
        const letterY = rect.top + rect.height / 2 - containerRect.top;

        const dist = Math.sqrt((mouseX - letterX) ** 2 + (mouseY - letterY) ** 2);

        if (dist < radius) {
          const norm = 1 - dist / radius; // linear falloff
          const wght = Math.round(400 + norm * 600); // 400 to 1000
          const opsz = Math.round(9 + norm * 31);   // 9 to 40
          el.style.fontVariationSettings = `'wght' ${wght}, 'opsz' ${opsz}`;
        } else {
          el.style.fontVariationSettings = "'wght' 400, 'opsz' 9";
        }
      });
    }
    requestAnimationFrame(animateProximity);
  }

  requestAnimationFrame(animateProximity);
</script>
