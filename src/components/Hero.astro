---
---

<section id="hero" class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 pt-10 pb-16 flex flex-col items-center">
  
  <!-- IDE Workstation Outer Container (Double-Bezel Architecture) -->
  <div class="w-full rounded-2xl bg-black/80 border border-emerald-500/30 p-2 shadow-[0_0_80px_rgba(16,185,129,0.12)] backdrop-blur-xl">
    
    <!-- VS Code Inner Frame -->
    <div id="vp-container" class="relative w-full rounded-xl bg-[#0b0c13]/95 border border-emerald-500/20 overflow-hidden font-mono">
      
      <!-- VS Code Window Title Bar -->
      <div class="px-4 py-2 bg-[#121420] border-b border-emerald-500/20 flex items-center justify-between text-xs text-emerald-300/80">
        <div class="flex items-center gap-2">
          <div class="flex items-center gap-1.5 mr-2">
            <span class="w-3 h-3 rounded-full bg-red-500/80 inline-block"></span>
            <span class="w-3 h-3 rounded-full bg-yellow-500/80 inline-block"></span>
            <span class="w-3 h-3 rounded-full bg-green-500/80 inline-block"></span>
          </div>
          <span class="font-bold text-emerald-400">📄 01_origin_story.md</span>
          <span class="text-[10px] text-emerald-500/60">— workspace/portfolio</span>
        </div>

        <div class="hidden sm:flex items-center gap-4 text-[11px] text-emerald-400/60">
          <span>UTF-8</span>
          <span>Markdown</span>
          <span> main*</span>
        </div>
      </div>

      <!-- COMPLETE 6-FILE WORKSPACE TABS HEADER -->
      <div class="flex items-center gap-1 px-3 pt-2 bg-[#0e101a] border-b border-emerald-500/20 text-xs overflow-x-auto no-scrollbar">
        <div class="px-3 py-1.5 rounded-t bg-[#0b0c13] border-t-2 border-emerald-400 text-emerald-300 font-bold flex items-center gap-2 flex-shrink-0">
          <span class="text-emerald-400">📝</span> 01_origin_story.md <span class="text-[10px] opacity-60">✕</span>
        </div>
        <a href="#academic" class="px-3 py-1.5 rounded-t text-emerald-300/60 hover:bg-[#121420] transition flex items-center gap-2 flex-shrink-0">
          <span class="text-amber-400">⚙️</span> 02_academic_journey.json
        </a>
        <a href="#culture" class="px-3 py-1.5 rounded-t text-emerald-300/60 hover:bg-[#121420] transition flex items-center gap-2 flex-shrink-0">
          <span class="text-teal-400">✨</span> 03_culture_hub.config
        </a>
        <a href="#skill-studio" class="px-3 py-1.5 rounded-t text-emerald-300/60 hover:bg-[#121420] transition flex items-center gap-2 flex-shrink-0">
          <span class="text-blue-400">⚡</span> 04_skill_studio.cpp
        </a>
        <a href="#n8n-studio" class="px-3 py-1.5 rounded-t text-emerald-300/60 hover:bg-[#121420] transition flex items-center gap-2 flex-shrink-0">
          <span class="text-purple-400">🤖</span> 05_n8n_workflow.py
        </a>
        <a href="#micro-tools" class="px-3 py-1.5 rounded-t text-emerald-300/60 hover:bg-[#121420] transition flex items-center gap-2 flex-shrink-0">
          <span class="text-emerald-400">🛠️</span> 06_micro_tools.css
        </a>
      </div>

      <!-- Editor Content Body with Line Numbers -->
      <div class="p-6 sm:p-10 flex gap-4 sm:gap-6 text-left">
        <!-- Line Numbers Column -->
        <div class="select-none text-emerald-500/30 text-xs sm:text-sm font-mono space-y-3 pt-1 text-right w-6 sm:w-8 flex-shrink-0">
          <div>01</div>
          <div>02</div>
          <div>03</div>
          <div>04</div>
          <div>05</div>
          <div>06</div>
          <div>07</div>
        </div>

        <!-- Markdown Code Content -->
        <div class="flex-1 min-w-0">
          <!-- Line 1: Status Badge comment -->
          <div class="text-xs sm:text-sm text-emerald-500/70 mb-3 font-mono">
            <span class="text-emerald-400 font-bold">// STATUS:</span> <span class="px-2 py-0.5 rounded bg-emerald-500/10 border border-emerald-500/30 text-emerald-300 inline-block" data-i18n="badge_status">AI Graduate @ MMU | CGPA 3.74</span>
          </div>

          <!-- Line 2 & 3: H1 VariableProximity Title -->
          <h1 class="text-2xl sm:text-4xl lg:text-5xl font-bold tracking-tight mb-6 leading-tight select-none cursor-default font-['Roboto_Flex']">
            <span class="text-emerald-500 font-mono font-normal mr-2 text-xl sm:text-3xl">#</span>
            <span class="vp-word text-white inline-block whitespace-nowrap" data-word="Full-Stack">Full-Stack</span>{" "}
            <span class="vp-word text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 via-teal-300 to-emerald-500 font-extrabold inline-block whitespace-nowrap" data-word="Developer">Developer</span>{" "}
            <br class="hidden sm:inline" />
            <span class="vp-word text-white inline-block whitespace-nowrap" data-word="with">with</span>{" "}
            <span class="vp-word text-white inline-block whitespace-nowrap" data-word="a">a</span>{" "}
            <span class="vp-word text-white inline-block whitespace-nowrap" data-word="Deep">Deep</span>{" "}
            <span class="vp-word text-white inline-block whitespace-nowrap" data-word="Passion">Passion</span>{" "}
            <span class="vp-word text-white inline-block whitespace-nowrap" data-word="for">for</span>{" "}
            <span class="vp-word text-amber-400 font-extrabold inline-block whitespace-nowrap" data-word="AI">AI</span>
          </h1>

          <!-- Line 4 & 5: Quote / Desc Block -->
          <div class="border-l-2 border-emerald-500/40 pl-4 py-1 mb-8 text-emerald-200/90 text-sm sm:text-base font-sans leading-relaxed">
            <p data-i18n="hero_desc">
              I'm <strong class="text-emerald-400 font-mono">Lim Wei Jian</strong>, an AI Graduate from MMU (CGPA 3.74) with a deep passion for full-stack engineering. Whether it's C++, Python, or TypeScript, I just enjoy turning ideas into smooth, useful software. Take a look around!
            </p>
          </div>

          <!-- Line 6: Interactive IDE Actions -->
          <div class="flex flex-wrap items-center gap-4 font-mono text-xs sm:text-sm pt-2">
            <a href="#skill-studio" class="px-5 py-2.5 rounded-lg bg-emerald-500 text-black font-bold hover:bg-emerald-400 transition shadow-lg shadow-emerald-500/20 flex items-center gap-2" data-i18n="btn_skills">
              ▶ Explore Skill Studio (C++/Py)
            </a>
            <a href="#n8n-studio" class="px-5 py-2.5 rounded-lg bg-emerald-950/80 border border-emerald-500/40 text-emerald-300 font-bold hover:bg-emerald-900/60 transition flex items-center gap-2" data-i18n="btn_workflow">
              ⚡ Trigger AI Workflow Node
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // VariableProximity Algorithm for Roboto Flex Variable Font
  const container = document.getElementById('vp-container');
  const words = document.querySelectorAll('.vp-word');
  const radius = 120; // proximity radius in px

  words.forEach(word => {
    const text = word.textContent || '';
    word.innerHTML = '';
    text.split('').forEach(char => {
      const span = document.createElement('span');
      span.className = 'vp-letter inline-block transition-transform duration-75';
      span.style.fontVariationSettings = "'wght' 400, 'opsz' 9";
      span.textContent = char;
      word.appendChild(span);
    });
  });

  const letters = document.querySelectorAll('.vp-letter');
  let mouseX = -999;
  let mouseY = -999;

  window.addEventListener('mousemove', (e) => {
    if (!container) return;
    const rect = container.getBoundingClientRect();
    mouseX = e.clientX - rect.left;
    mouseY = e.clientY - rect.top;
  });

  function animateProximity() {
    if (container) {
      const containerRect = container.getBoundingClientRect();

      letters.forEach(letter => {
        const el = letter as HTMLElement;
        const rect = el.getBoundingClientRect();
        const letterX = rect.left + rect.width / 2 - containerRect.left;
        const letterY = rect.top + rect.height / 2 - containerRect.top;

        const dist = Math.sqrt((mouseX - letterX) ** 2 + (mouseY - letterY) ** 2);

        if (dist < radius) {
          const norm = 1 - dist / radius;
          const wght = Math.round(400 + norm * 600);
          const opsz = Math.round(9 + norm * 31);
          el.style.fontVariationSettings = `'wght' ${wght}, 'opsz' ${opsz}`;
        } else {
          el.style.fontVariationSettings = "'wght' 400, 'opsz' 9";
        }
      });
    }
    requestAnimationFrame(animateProximity);
  }

  requestAnimationFrame(animateProximity);
</script>
